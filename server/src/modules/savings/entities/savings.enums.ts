export enum AdjustmentPolicy {
  NONE = "NONE",
  NEXT = "NEXT",
  PREVIOUS = "PREVIOUS",
}

export enum RecurrenceType {
  DAILY = "DAILY",
  WEEKLY = "WEEKLY",
  MONTHLY = "MONTHLY",
}

export enum SavingsEndType {
  INDEFINITE = "INDEFINITE",
  DATE = "DATE",
}

export enum DepositStatus {
  SCHEDULED = "SCHEDULED",
  COMPLETED = "COMPLETED",
  SKIPPED = "SKIPPED",
  CANCELLED = "CANCELLED",
}
